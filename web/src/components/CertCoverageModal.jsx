// CertCoverageModal retired. The "ensure a covering cert for this route"
// flow lived alongside the old per-route gating model. Routes are now
// derived from acl+rule pairs; cert coverage is managed directly from the
// Certificates page (edit cert, expand SANs). This stub is left so stale
// imports fail loudly.
