// NetworkGroupEditModal retired. NetworkGroups are no longer a state
// primitive — express network grouping as ACLs with field=src (Network
// group in the ACL field-picker). This file is left as a stub so stale
// imports fail loudly rather than silently going through.
