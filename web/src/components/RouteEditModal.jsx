// RouteEditModal retired. Routes are no longer a first-class primitive;
// they're derived (lens view) from acl+rule pairs and created via the
// wizard in `components/RouteWizard.jsx`. The Routes page lens links into
// the ACLs and Rules pages for edits. This file is left as a stub so
// stale imports break loudly with `RouteEditModal is not a function`
// rather than silently going through.
